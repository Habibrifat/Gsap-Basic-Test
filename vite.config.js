// vite.config.js
import { defineConfig } from "vite";

export default defineConfig({
  root: "src", // Set root directory if your index.html is in 'src'
  publicDir: "../public", // Directory for static assets
});
